/**
 * LRU( Least recently used )
 */

const LIMIT = 20;

export default class LRUCache {
    constructor(sqList, limit = LIMIT){
        this.limit = limit;
        this.size = 0;

    }
    
    put(key, value){
        
    }
    
    find(value){}
    
    set(key, value){
        
    }

    toJS(){}

    toObject(){}
}